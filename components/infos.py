from dash import html
import dash_bootstrap_components as dbc

from app import app

# ============ Layout ============ #
layout = dbc.Col([
    html.Legend("Info")
])

# ============ Callbacks ============ #